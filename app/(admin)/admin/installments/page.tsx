import{AdminInstallmentsView}from '@/components/admin-installments-view';import{requireAdmin}from '@/lib/auth';import type{Installment}from '@/lib/types';export default async function Page(){const{supabase}=await requireAdmin();const{data}=await supabase.from('loan_installments').select('*, profiles(full_name,email,phone), loan_applications(application_no)').in('status',['submitted','rejected','paid']).order('updated_at',{ascending:false});return <AdminInstallmentsView items={(data??[]) as unknown as Installment[]}/>}
