import{requireAdmin}from'@/lib/auth';import{NotificationsView}from'@/components/notifications-view';import type{Notification}from'@/lib/types';export default async function Page(){const{supabase}=await requireAdmin();const{data}=await supabase.from('app_notifications').select('*').order('created_at',{ascending:false}).limit(200);return <NotificationsView notifications={(data??[]) as Notification[]}/>}
