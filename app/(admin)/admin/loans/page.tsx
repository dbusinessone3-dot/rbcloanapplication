import { AdminLoansView } from '@/components/admin-loans-view';import { requireAdmin } from '@/lib/auth';import type { LoanApplication } from '@/lib/types';export default async function AdminLoansPage(){const{supabase}=await requireAdmin();const{data}=await supabase.from('loan_applications').select('*, profiles(full_name,email,phone)').order('created_at',{ascending:false});return <AdminLoansView loans={(data??[]) as unknown as LoanApplication[]}/>;}
